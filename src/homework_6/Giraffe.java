package homework_6;

public class Giraffe extends Wild{

    public Giraffe(int id, int age, int weight, boolean predator,Colours colours) {

        setId(id);
        setAge(age);
        setWeight(weight);
        setPredator(predator);
        setColour(colours);
    }
}
